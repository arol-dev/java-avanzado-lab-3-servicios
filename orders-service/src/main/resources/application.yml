server:
  port: 8082
spring:
  application:
    name: orders-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

feign:
  httpclient:
    enabled: true
  circuitbreaker:
    enabled: false
